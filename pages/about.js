import Layout from "@containers/Layout";

function about(props) {
    const title = 'About'
    return (
        <Layout title={title}>
            <div>
                Acerca de
            </div>
        </Layout>
    );
}

export default about;